/**
 * Returns the input array arr permuted into a zigzag array. A array where numbers increase then decrease in size after index k by k = (n + 1) / 2. n = length of array.
 *
 * @param {Array.<number>} arr  - Input array of numbers.
 * @returns {Array.<number>} - Returns permuted array of numbers representing the zigzag array.
 */
export const zigzag = (arr: number[]): number[] => {
    const indexToDecreaseAfter = (arr.length + 1) / 2;
  return [1];
};
